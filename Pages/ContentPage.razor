@page "/"
@page "/{Tenant}/content/{Slug}"

<HeadContent>
    <meta name="description" content="@(CurrentEntry?.Seo?.Description ?? "STC STORB Consulting - Entdecken Sie unsere Cloud-, Dokumentations- und E-Commerce-Dienstleistungen.")" />
    <meta name="robots" content="@(CurrentEntry?.Seo?.Robots ?? " index,follow")" />
    <meta property="og:title" content="@(CurrentEntry?.Seo?.Title ?? CurrentEntry?.Title ?? "STC STORB Consulting")" />
    <meta property="og:description" content="@(CurrentEntry?.Seo?.Description ?? "STC STORB Consulting - Entdecken Sie unsere Cloud-, Dokumentations- und E-Commerce-Dienstleistungen.")" />
    <meta property="og:type" content="website" />
</HeadContent>

<PageTitle>@PageTitle</PageTitle>

<RadzenMarkdown @key="@($"{Tenant}-{Slug}")" Text="@markdownContent" AllowHtml="@allowHtml" />
